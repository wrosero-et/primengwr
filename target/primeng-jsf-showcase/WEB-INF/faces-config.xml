<?xml version="1.0" encoding="UTF-8"?>
<faces-config xmlns="http://xmlns.jcp.org/xml/ns/javaee"
              xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
              xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee 
              http://xmlns.jcp.org/xml/ns/javaee/web-facesconfig_2_3.xsd"
              version="2.3">

    <!-- Configuración de la Aplicación JSF -->
    <application>
        <!-- Configuración de recursos (CSS, JS, imágenes) -->
        <resource-bundle>
            <base-name>messages</base-name>
            <var>msg</var>
        </resource-bundle>
        
        <!-- Locale por defecto -->
        <locale-config>
            <default-locale>es</default-locale>
            <supported-locale>en</supported-locale>
            <supported-locale>es</supported-locale>
        </locale-config>
        
        <!-- Configuración del View Handler para mejor performance -->
        <default-render-kit-id>HTML_BASIC</default-render-kit-id>
    </application>

    <!-- Configuración del ciclo de vida de JSF -->
    <lifecycle>
        <!-- Listener para debugging (comentar en producción) -->
        <!-- <phase-listener>com.primeng.util.DebugPhaseListener</phase-listener> -->
    </lifecycle>

</faces-config>
